import React from "react";
import styles from '../styles/home.module.css';

const Home = () => {
    return (
        <div className={styles.homeContainer}>
            <h1 className={styles.homeText}>Welcome to the All-Exchange! The Market of the Future!</h1>
        </div>
    );
}

export default Home;